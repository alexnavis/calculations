'use strict';
const DEFAULT = require('./default.json');
const NUMERICAL = require('./numerical.json');
const MULTIPLE = require('./multiple.json');
const CONDITIONAL = require('./conditional.json');
const VARIABLES = require('./variables.json');

module.exports = {
  DEFAULT,
  NUMERICAL,
  MULTIPLE,
  CONDITIONAL,
  VARIABLES,
}